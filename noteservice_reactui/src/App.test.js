import React from 'react';
import { render } from '@testing-library/react';
import App from './App';

// Testerna funkar inte p.g.a. Quill editorn, det finns ett beroende till en senare version av JDDOM. Den kommer
// troligen att uppdateras i en senare vesion av CRA'n.

test('Load a note', () => {

});

test('Save a new note', () => {
});




  //const { getByText } = render(<App />);
  //const linkElement = getByText(/learn react/i);
  //expect(linkElement).toBeInTheDocument();
